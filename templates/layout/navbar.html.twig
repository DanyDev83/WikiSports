<nav class="navbar navbar-expand-sm navbar-dark">
    <div class="container">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mynavbar">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="mynavbar">
            <ul class="d-flex navbar-nav w-100 justify-content-between">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}"><i class="fa-solid fa-house-chimney"></i> Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_categories') }}"><i class="fa-solid fa-bicycle"></i>
                        Catégories Sportives</a>
                </li>
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown">
                            <i class="fa-solid fa-user"></i> {{ app.user.username }}</a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item text-success" href="{{ path('app_account') }}">Mon compte</a>
                            </li>
                            <li><a class="dropdown-item text-success" href="{{ path('app_newArticle') }}">Créer un
                                    article</a></li>
                            {% if is_granted('ROLE_ADMIN') %}
                                <li><a class="dropdown-item text-info" href="{{ path('app_admin') }}">Administration</a></li>
                            {% endif %}
                            <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item d-flex">
                        <a class="nav-link" href="{{ path('app_login') }}"> <i class="fa-solid fa-user"></i> Connexion</a>
                        <a class="nav-link" href="{{ path('app_register') }}"> <i class="fa-solid fa-pen-to-square"></i> Inscription</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>